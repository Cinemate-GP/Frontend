import LoginForm from "@/components/auth/LoginForm";
import React from "react";

const page = () => {
  return (
    <div className="text-center">
      <LoginForm />
    </div>
  );
};

export default page;
